package com.who.onecupafterwork.group;

import android.os.Bundle;
import android.support.v4.app.Fragment;
import android.view.LayoutInflater;
import android.view.View;
import android.view.ViewGroup;

import com.who.onecupafterwork.signin.R;

public class Fragment0_WaitStartScreen extends Fragment {
	/* 가입 승인을 기다리는 화면 */
	/* 가입 승인을 기다리고 있습니다. */

	@Override
	public View onCreateView(LayoutInflater inflater, ViewGroup container, Bundle savedInstanceState) {
		View v = inflater.inflate(R.layout.fragment0_wait_start_screen, container, false);
		/*v.setOnClickListener(new View.OnClickListener() {

			@Override
			public void onClick(View v) {
				FragmentManager fm = getParentFragment().getChildFragmentManager();
				FragmentTransaction ft = fm.beginTransaction();
				ft.replace(R.id.fragment1_frame, new Fragment1_AddSame());
				ft.commit();
			}
		});*/
		return v;
	}
}
